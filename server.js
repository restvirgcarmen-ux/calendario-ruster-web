const express=require("express"),path=require("path");const app=express();const PORT=process.env.PORT||3000;const APK_URL=process.env.APK_URL||"/downloads/calendario-ruster.apk";const PRICE=process.env.PRODUCT_PRICE||"29.90";const CURRENCY=process.env.CURRENCY||"PEN";app.use(express.json());app.use(express.urlencoded({extended:true}));app.use(express.static(path.join(__dirname,"public")));app.get("/api/config",(q,r)=>r.json({ok:true,product:"Calendario Ruster",price:PRICE,currency:CURRENCY,apkUrl:APK_URL}));app.post("/api/order",(q,r)=>{const{name,email}=q.body||{};if(!name||!email)return r.status(400).json({ok:false,message:"Faltan datos del comprador."});r.json({ok:true,status:"pending_payment",message:"Pedido preparado. El pago real se conectará en el siguiente paso.",order:{name,email,product:"Calendario Ruster",price:PRICE,currency:CURRENCY}})});app.get("/download",(q,r)=>r.redirect(APK_URL));app.get("*",(q,r)=>r.sendFile(path.join(__dirname,"public","index.html")));app.listen(PORT,()=>console.log("Calendario Ruster Store en puerto "+PORT));